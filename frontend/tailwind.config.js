/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  darkMode: 'class', // Enables manual dark mode toggling via a 'dark' class on the HTML tag
  theme: {
    extend: {},
  },
  plugins: [],
}